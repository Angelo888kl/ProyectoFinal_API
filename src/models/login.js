/**
 * Clase Form login
 */
export class fLogin {
    constructor(username, password) {
        this.username = username;
        this.password = password;
    }
}

/**
 * Clase login
 */
export class mLogin {
    constructor(usuario, fecha, estado) {
        this.usuario = usuario;
        this.fecha = fecha;
        this.estado = estado;
    }
}